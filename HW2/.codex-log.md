› Write tests for @filename



















































• I’ll scaffold the client and server apps, set up dev/test scripts, add
  integration tests, and include docs under docs/source_materials and docs/
  TODO. Then I’ll wire up the root package.json and Dockerfile.

• Added .gitignore (+19 -0)
     1 +# Node
     2 +node_modules/
     3 +dist/
     4 +.cache/
     5 +npm-debug.log*
     6 +
     7 +# Env/OS
     8 +.env
     9 +.env.*
    10 +.DS_Store
    11 +
    12 +# Editor/IDE
    13 +.idea/
    14 +.vscode/
    15 +
    16 +# Project
    17 +server/public/
    18 +data/
    19 +.venv/

• Added package.json (+20 -0)
     1 +{
     2 +  "name": "hw2-coding-interview-platform",
     3 +  "private": true,
     4 +  "version": "0.1.0",
     5 +  "scripts": {
     6 +    "dev:client": "npm --prefix client run dev",
     7 +    "dev:server": "npm --prefix server run dev",
     8 +    "dev": "concurrently -k -n client,server -c green,blue \"npm:dev
        :client\" \"npm:dev:server\"",
     9 +    "build": "npm --prefix client run build",
    10 +    "test": "npm --prefix server test"
    11 +  },
    12 +  "devDependencies": {
    13 +    "concurrently": "^8.2.2",
    14 +    "wait-on": "^7.0.1"
    15 +  },
    16 +  "workspaces": [
    17 +    "client",
    18 +    "server"
    19 +  ]
    20 +}

• Added server/package.json (+24 -0)
     1 +{
     2 +  "name": "server",
     3 +  "version": "0.1.0",
     4 +  "private": true,
     5 +  "type": "commonjs",
     6 +  "main": "index.js",
     7 +  "scripts": {
     8 +    "dev": "nodemon index.js",
     9 +    "start": "node index.js",
    10 +    "test": "vitest run --reporter=verbose",
    11 +    "test:watch": "vitest"
    12 +  },
    13 +  "dependencies": {
    14 +    "cors": "^2.8.5",
    15 +    "express": "^4.18.2",
    16 +    "socket.io": "^4.7.2"
    17 +  },
    18 +  "devDependencies": {
    19 +    "nodemon": "^3.0.0",
    20 +    "socket.io-client": "^4.7.2",
    21 +    "supertest": "^6.3.3",
    22 +    "vitest": "^1.3.1"
    23 +  }
    24 +}

• Added server/index.js (+49 -0)
     1 +const express = require('express');
     2 +const http = require('http');
     3 +const path = require('path');
     4 +const fs = require('fs');
     5 +const cors = require('cors');
     6 +const { Server } = require('socket.io');
     7 +
     8 +const app = express();
     9 +app.use(cors({ origin: true, credentials: true }));
    10 +app.use(express.json());
    11 +
    12 +app.get('/health', (req, res) => res.json({ ok: true }));
    13 +
    14 +// Serve built client if present
    15 +const publicDir = path.join(__dirname, 'public');
    16 +if (fs.existsSync(publicDir)) {
    17 +  app.use(express.static(publicDir));
    18 +  app.get('*', (req, res) => res.sendFile(path.join(publicDir, 'inde
        x.html')));
    19 +}
    20 +
    21 +const server = http.createServer(app);
    22 +const io = new Server(server, {
    23 +  cors: { origin: '*', methods: ['GET', 'POST'] }
    24 +});
    25 +
    26 +io.on('connection', (socket) => {
    27 +  socket.on('join', (sessionId) => {
    28 +    if (!sessionId) return;
    29 +    socket.join(`session:${sessionId}`);
    30 +    socket.emit('joined', { sessionId });
    31 +  });
    32 +
    33 +  socket.on('code:update', (payload) => {
    34 +    try {
    35 +      const { sessionId, code, lang, cursor } = payload || {};
    36 +      if (!sessionId) return;
    37 +      socket.to(`session:${sessionId}`).emit('code:update', { code,
        lang, cursor });
    38 +    } catch (_) {}
    39 +  });
    40 +});
    41 +
    42 +const PORT = process.env.PORT || 8080;
    43 +if (require.main === module) {
    44 +  server.listen(PORT, () => {
    45 +    console.log(`[server] listening on :${PORT}`);
    46 +  });
    47 +}
    48 +
    49 +module.exports = { app, server };

• Added server/vitest.config.mjs (+10 -0)
     1 +import { defineConfig } from 'vitest/config';
     2 +
     3 +export default defineConfig({
     4 +  test: {
     5 +    environment: 'node',
     6 +    bail: 1,
     7 +    timeout: 10000,
     8 +    hookTimeout: 10000
     9 +  }
    10 +});

• Added server/tests/socket.test.js (+64 -0)
     1 +const { io } = require('socket.io-client');
     2 +const request = require('supertest');
     3 +
     4 +describe('server', () => {
     5 +  let httpServer, app, address;
     6 +
     7 +  beforeAll(async () => {
     8 +    const mod = require('..');
     9 +    httpServer = mod.server;
    10 +    app = mod.app;
    11 +    await new Promise((resolve) => {
    12 +      if (httpServer.listening) return resolve();
    13 +      httpServer.listen(0, resolve);
    14 +    });
    15 +    const { port } = httpServer.address();
    16 +    address = `http://localhost:${port}`;
    17 +  });
    18 +
    19 +  afterAll(async () => {
    20 +    if (!httpServer) return;
    21 +    await new Promise((resolve) => httpServer.close(resolve));
    22 +  });
    23 +
    24 +  it('health endpoint responds', async () => {
    25 +    await request(app).get('/health').expect(200).then((r) => {
    26 +      expect(r.body.ok).toBe(true);
    27 +    });
    28 +  });
    29 +
    30 +  it('broadcasts code:update within a session', async () => {
    31 +    const sessionId = 'test-session-1';
    32 +    const c1 = io(address, { transports: ['websocket'] });
    33 +    const c2 = io(address, { transports: ['websocket'] });
    34 +
    35 +    await new Promise((r) => c1.on('connect', r));
    36 +    await new Promise((r) => c2.on('connect', r));
    37 +    c1.emit('join', sessionId);
    38 +    c2.emit('join', sessionId);
    39 +
    40 +    // small join delay
    41 +    await new Promise((r) => setTimeout(r, 100));
    42 +
    43 +    const expected = 'print(1)';
    44 +    const got = new Promise((resolve, reject) => {
    45 +      const to = setTimeout(() => reject(new Error('timeout')), 2000
        );
    46 +      c2.on('code:update', (payload) => {
    47 +        try {
    48 +          if (payload.code === expected) {
    49 +            clearTimeout(to);
    50 +            resolve(payload);
    51 +          }
    52 +        } catch (e) { /* ignore */ }
    53 +      });
    54 +    });
    55 +
    56 +    c1.emit('code:update', { sessionId, code: expected, lang: 'pytho
        n' });
    57 +    const payload = await got;
    58 +    expect(payload.lang).toBe('python');
    59 +
    60 +    c1.close();
    61 +    c2.close();
    62 +  });
    63 +});
    64 +

• Added client/package.json (+23 -0)
     1 +{
     2 +  "name": "client",
     3 +  "version": "0.1.0",
     4 +  "private": true,
     5 +  "type": "module",
     6 +  "scripts": {
     7 +    "dev": "vite",
     8 +    "build": "vite build",
     9 +    "preview": "vite preview --strictPort --port 5173"
    10 +  },
    11 +  "dependencies": {
    12 +    "@codemirror/lang-javascript": "^6.1.9",
    13 +    "@codemirror/lang-python": "^6.1.3",
    14 +    "@uiw/react-codemirror": "^4.22.0",
    15 +    "react": "^18.2.0",
    16 +    "react-dom": "^18.2.0",
    17 +    "socket.io-client": "^4.7.2"
    18 +  },
    19 +  "devDependencies": {
    20 +    "@vitejs/plugin-react": "^4.2.0",
    21 +    "vite": "^5.0.4"
    22 +  }
    23 +}

• Added client/index.html (+12 -0)
     1 +<!doctype html>
     2 +<html lang="en">
     3 +  <head>
     4 +    <meta charset="UTF-8" />
     5 +    <meta name="viewport" content="width=device-width, initial-scale
        =1.0" />
     6 +    <title>HW2 Coding Interview</title>
     7 +  </head>
     8 +  <body>
     9 +    <div id="root"></div>
    10 +    <script type="module" src="/src/main.tsx"></script>
    11 +  </body>
    12 +  </html>

• Added client/vite.config.ts (+10 -0)
     1 +import { defineConfig } from 'vite';
     2 +import react from '@vitejs/plugin-react';
     3 +
     4 +export default defineConfig({
     5 +  plugins: [react()],
     6 +  server: {
     7 +    port: 5173,
     8 +    strictPort: true
     9 +  }
    10 +});

• Added client/src/main.tsx (+9 -0)
    1 +import React from 'react'
    2 +import ReactDOM from 'react-dom/client'
    3 +import App from './App'
    4 +
    5 +ReactDOM.createRoot(document.getElementById('root') as HTMLElement).r
       ender(
    6 +  <React.StrictMode>
    7 +    <App />
    8 +  </React.StrictMode>
    9 +)

• Added client/src/App.tsx (+141 -0)
      1 +import React, { useEffect, useMemo, useRef, useState } from 'react'
      2 +import { io, Socket } from 'socket.io-client'
      3 +import CodeMirror from '@uiw/react-codemirror'
      4 +import { javascript } from '@codemirror/lang-javascript'
      5 +import { python } from '@codemirror/lang-python'
      6 +
      7 +type Lang = 'javascript' | 'python'
      8 +
      9 +const serverUrl = (import.meta as any).env?.VITE_API_BASE || 'http:
         //localhost:8080'
     10 +
     11 +function getOrCreateSessionId() {
     12 +  const url = new URL(window.location.href)
     13 +  let s = url.searchParams.get('s')
     14 +  if (!s) {
     15 +    s = Math.random().toString(36).slice(2, 10)
     16 +    url.searchParams.set('s', s)
     17 +    history.replaceState(null, '', url.toString())
     18 +  }
     19 +  return s
     20 +}
     21 +
     22 +export default function App() {
     23 +  const [lang, setLang] = useState<Lang>('javascript')
     24 +  const [code, setCode] = useState<string>('// Write some code\ncon
         sole.log("Hello")')
     25 +  const [output, setOutput] = useState<string>('')
     26 +  const [sessionId] = useState<string>(getOrCreateSessionId())
     27 +  const socketRef = useRef<Socket | null>(null)
     28 +  const applyingRemoteRef = useRef(false)
     29 +
     30 +  useEffect(() => {
     31 +    const socket = io(serverUrl, { transports: ['websocket'] })
     32 +    socketRef.current = socket
     33 +    socket.on('connect', () => {
     34 +      socket.emit('join', sessionId)
     35 +    })
     36 +    socket.on('code:update', (payload: any) => {
     37 +      if (typeof payload?.code === 'string') {
     38 +        applyingRemoteRef.current = true
     39 +        setCode(payload.code)
     40 +        setTimeout(() => (applyingRemoteRef.current = false), 0)
     41 +      }
     42 +      if (payload?.lang) setLang(payload.lang as Lang)
     43 +    })
     44 +    return () => {
     45 +      socket.disconnect()
     46 +    }
     47 +  }, [sessionId])
     48 +
     49 +  const extensions = useMemo(() => {
     50 +    return lang === 'javascript' ? [javascript()] : [python()]
     51 +  }, [lang])
     52 +
     53 +  const onChange = (value: string) => {
     54 +    setCode(value)
     55 +    if (applyingRemoteRef.current) return
     56 +    socketRef.current?.emit('code:update', { sessionId, code: value
         , lang })
     57 +  }
     58 +
     59 +  const runCode = async () => {
     60 +    setOutput('Running...')
     61 +    if (lang === 'javascript') {
     62 +      const res = await runJS(code)
     63 +      setOutput(res.output)
     64 +    } else {
     65 +      const res = await runPy(code)
     66 +      setOutput(res.output)
     67 +    }
     68 +  }
     69 +
     70 +  const shareUrl = window.location.href
     71 +
     72 +  return (
     73 +    <div style={{ maxWidth: 960, margin: '1rem auto', padding: '0 1
         rem', fontFamily: 'ui-sans-serif, system-ui' }}>
     74 +      <h2>Realtime Coding Interview</h2>
     75 +      <div style={{ display: 'flex', gap: 8, alignItems: 'center',
         marginBottom: 8 }}>
     76 +        <label>Session:</label>
     77 +        <code>{sessionId}</code>
     78 +        <button onClick={() => navigator.clipboard.writeText(shareU
         rl)}>Copy Link</button>
     79 +        <label style={{ marginLeft: 'auto' }}>Lang:</label>
     80 +        <select value={lang} onChange={(e) => setLang(e.target.valu
         e as Lang)}>
     81 +          <option value="javascript">JavaScript</option>
     82 +          <option value="python">Python</option>
     83 +        </select>
     84 +        <button onClick={runCode}>Run</button>
     85 +      </div>
     86 +      <CodeMirror value={code} height="400px" extensions={extension
         s} onChange={onChange} basicSetup={{ lineNumbers: true }} />
     87 +      <div style={{ marginTop: 12 }}>
     88 +        <h3>Output</h3>
     89 +        <pre style={{ background: '#f5f5f5', padding: 12, minHeight
         : 120, overflowX: 'auto' }}>{output}</pre>
     90 +      </div>
     91 +    </div>
     92 +  )
     93 +}
     94 +
     95 +async function runJS(source: string): Promise<{ ok: boolean; output
         : string }> {
     96 +  return new Promise((resolve) => {
     97 +    const worker = new Worker(new URL('./workers/jsRunner.js', impo
         rt.meta.url), { type: 'module' })
     98 +    const to = setTimeout(() => {
     99 +      worker.terminate()
    100 +      resolve({ ok: false, output: 'Timeout' })
    101 +    }, 5000)
    102 +    worker.onmessage = (e) => {
    103 +      clearTimeout(to)
    104 +      worker.terminate()
    105 +      resolve({ ok: e.data?.ok, output: e.data?.output ?? '' })
    106 +    }
    107 +    worker.postMessage({ code: source })
    108 +  })
    109 +}
    110 +
    111 +let pyWorker: Worker | null = null
    112 +let pyReady = false
    113 +function ensurePyWorker() {
    114 +  if (!pyWorker) {
    115 +    pyWorker = new Worker(new URL('./workers/pyodideWorker.js', imp
         ort.meta.url))
    116 +    pyWorker.onmessage = (e) => {
    117 +      if (e.data?.type === 'ready') pyReady = true
    118 +    }
    119 +    pyWorker.postMessage({ type: 'init' })
    120 +  }
    121 +}
    122 +
    123 +async function runPy(source: string): Promise<{ ok: boolean; output
         : string }> {
    124 +  ensurePyWorker()
    125 +  return new Promise((resolve) => {
    126 +    const waitReady = setInterval(() => {
    127 +      if (pyReady) {
    128 +        clearInterval(waitReady)
    129 +        const to = setTimeout(() => resolve({ ok: false, output: 'T
         imeout' }), 8000)
    130 +        pyWorker!.onmessage = (e) => {
    131 +          if (e.data?.type === 'result') {
    132 +            clearTimeout(to)
    133 +            resolve({ ok: e.data.ok, output: e.data.output })
    134 +          }
    135 +        }
    136 +        pyWorker!.postMessage({ type: 'run', code: source })
    137 +      }
    138 +    }, 100)
    139 +  })
    140 +}
    141 +

• Added client/src/workers/jsRunner.js (+20 -0)
     1 +self.onmessage = (e) => {
     2 +  const code = (e && e.data && e.data.code) || ''
     3 +  try {
     4 +    const logs = []
     5 +    const origLog = console.log
     6 +    console.log = (...args) => logs.push(args.map(String).join(' '))
     7 +    let result
     8 +    try {
     9 +      // eslint-disable-next-line no-eval
    10 +      result = eval(code)
    11 +    } finally {
    12 +      console.log = origLog
    13 +    }
    14 +    if (result !== undefined) logs.push(String(result))
    15 +    self.postMessage({ ok: true, output: logs.join('\n') })
    16 +  } catch (err) {
    17 +    self.postMessage({ ok: false, output: `Error: ${err && err.messa
        ge ? err.message : String(err)}` })
    18 +  }
    19 +}
    20 +

• Added client/src/workers/pyodideWorker.js (+25 -0)
     1 +/* classic worker to load Pyodide from CDN */
     2 +self.onmessage = async (e) => {
     3 +  const data = e && e.data
     4 +  if (!data) return
     5 +  if (data.type === 'init') {
     6 +    try {
     7 +      // Pin a version that is known stable
     8 +      self.importScripts('https://cdn.jsdelivr.net/pyodide/v0.24.1/f
        ull/pyodide.js')
     9 +      // @ts-ignore
    10 +      self.pyodide = await self.loadPyodide({ indexURL: 'https://cdn
        .jsdelivr.net/pyodide/v0.24.1/full/' })
    11 +      self.postMessage({ type: 'ready' })
    12 +    } catch (err) {
    13 +      self.postMessage({ type: 'result', ok: false, output: `Pyodide
         init error: ${String(err)}` })
    14 +    }
    15 +  } else if (data.type === 'run') {
    16 +    try {
    17 +      // @ts-ignore
    18 +      const result = await self.pyodide.runPythonAsync(data.code)
    19 +      self.postMessage({ type: 'result', ok: true, output: String(re
        sult ?? '') })
    20 +    } catch (err) {
    21 +      self.postMessage({ type: 'result', ok: false, output: String(e
        rr) })
    22 +    }
    23 +  }
    24 +}
    25 +

• Added Dockerfile (+25 -0)
     1 +# Build-and-run in single image
     2 +FROM node:20-alpine AS base
     3 +WORKDIR /app
     4 +
     5 +# Root deps for scripts
     6 +COPY package.json ./
     7 +RUN npm pkg get name >/dev/null 2>&1 || true
     8 +
     9 +# Client
    10 +COPY client/package.json client/package.json
    11 +RUN npm --prefix client install --no-audit --no-fund || true
    12 +COPY client/ client/
    13 +RUN npm --prefix client run build || true
    14 +
    15 +# Server
    16 +COPY server/package.json server/package.json
    17 +RUN npm --prefix server install --no-audit --no-fund || true
    18 +COPY server/ server/
    19 +
    20 +# Serve client from server/public
    21 +RUN mkdir -p server/public && cp -r client/dist/* server/public/ ||
        true
    22 +
    23 +EXPOSE 8080
    24 +WORKDIR /app/server
    25 +CMD ["node", "index.js"]

• Added docs/source_materials/homework.md (+177 -0)
      1 +# End-to-End Application Development
      2 +
      3 +In this homework, we'll build an end-to-end application with AI.
      4 +
      5 +You can use any tool you want: ChatGPT, Claude, GitHub Copilot, Cod
         ex, Cursor, Antigravity, etc.
      6 +
      7 +With chat-based applications you will need to copy code back-and-fo
         rth, so we recommend that you use an AI assistant in your IDE with
         agent mode.
      8 +
      9 +We will implement a platform for online coding interviews.
     10 +
     11 +The app should be able to do the following:
     12 +
     13 +- Create a link and share it with candidates
     14 +- Allow everyone who connects to edit code in the code panel
     15 +- Show real-time updates to all connected users
     16 +- Support syntax highlighting for multiple languages
     17 +- Execute code safely in the browser
     18 +
     19 +You can choose any technologies you want. For example:
     20 +
     21 +- Frontend: React + Vite
     22 +- Backend: Express.js
     23 +
     24 +We recommend using JavaScript for frontend, because with other tech
         nologies, some of the homework requirements may be difficult to imp
         lement.
     25 +
     26 +But you can experiment with alternatives, such as Streamlit.
     27 +
     28 +You don't need to know these technologies for doing this homework.
     29 +
     30 +
     31 +## Question 1: Initial Implementation
     32 +
     33 +Ask AI to implement both frontend and backend - in one prompt.
     34 +
     35 +Note: you can also follow the same path as in the videos and make i
         t in 3 steps:
     36 +
     37 +1. Frontend
     38 +2. OpenAPI specs
     39 +3. Backend
     40 +
     41 +What's the initial prompt you gave to AI to start the implementatio
         n?
     42 +
     43 +Copy and paste it in the homework form.
     44 +
     45 +
     46 +## Question 2: Integration Tests
     47 +
     48 +Maybe at this point your application will already function. Maybe n
         ot. But it's always a good idea to cover it with tests.
     49 +
     50 +We usually do it even before trying to run the application because
         it helps to resurface all the problems with implementation.
     51 +
     52 +Ask AI to write integration tests that check that the interaction b
         etween client and server works.
     53 +
     54 +Also it's a good idea to ask it to start creating a `README.md` fil
         e with all the commands for running and testing your application.
     55 +
     56 +What's the terminal command you use for executing tests?
     57 +
     58 +
     59 +
     60 +## Question 3: Running Both Client and Server
     61 +
     62 +Now let's make it possible to run both client and server at the sam
         e time. Use `concurrently` for that.
     63 +
     64 +What's the command you have in `package.json` for `npm dev` for run
         ning both?
     65 +
     66 +
     67 +## Question 4: Syntax Highlighting
     68 +
     69 +Let's now add support for syntax highlighting for JavaScript and Py
         thon.
     70 +
     71 +Which library did AI use for it?
     72 +
     73 +
     74 +
     75 +## Question 5: Code Execution
     76 +
     77 +Now let's add code execution.
     78 +
     79 +For security reasons, we don't want to execute code directly on the
          server. Instead, let's use WASM to execute the code only in the br
         owser.
     80 +
     81 +Which library did AI use for compiling Python to WASM?
     82 +
     83 +
     84 +## Question 6: Containerization
     85 +
     86 +Now let's containerize our application. Ask AI to help you create a
          Dockerfile for the application. Put both backend and frontend in o
         ne container.
     87 +
     88 +What's the base image you used for your Dockerfile?
     89 +
     90 +
     91 +## Question 7: Deployment
     92 +
     93 +Now let's deploy it. Choose a service to deploy your application.
     94 +
     95 +Which service did you use for deployment?
     96 +
     97 +
     98 +## Homework URL
     99 +
    100 +Commit your code to GitHub. You can create a repository for this co
         urse. Within the repository, create a folder, e.g. "02-coding-inter
         view", where you put the code.
    101 +
    102 +Use the link to this folder in the homework submission form.
    103 +
    104 +Don't forget to commit your code at every step. You can create an `
         AGENTS.md` file with the instructions for AI to help you with git c
         ommands.
    105 +
    106 +
    107 +## Tip
    108 +
    109 +You can copy-paste the homework description into the AI system of y
         our choice. But make sure you understand (and follow) all the steps
          in the response.
    110 +
    111 +
    112 +## Submission
    113 +
    114 +Submit your homework here: https://courses.datatalks.club/ai-dev-to
         ols-2025/homework/hw2
    115 +
    116 +
    117 +## Learning in Public
    118 +
    119 +We encourage everyone to share what they learned.
    120 +
    121 +Don't worry about being perfect. Everyone starts somewhere, and peo
         ple love following genuine learning journeys!
    122 +
    123 +### Recording a Demo Video
    124 +
    125 +Consider recording a short demo video of your application in action
         ! This makes your post much more engaging and helps others see what
          you've built.
    126 +
    127 +You can use:
    128 +- Screen recording tools like OBS Studio, QuickTime, or Windows Gam
         e Bar
    129 +- Loom for quick and shareable recording
    130 +- Snapping Tool on Windowns
    131 +
    132 +Keep it short (30-90 seconds) and show:
    133 +- Creating a coding session link
    134 +- Multiple users editing code simultaneously
    135 +- Real-time updates across browsers
    136 +- Code execution in action
    137 +
    138 +Upload your video to LinkedIn, Twitter/X, or YouTube and share the
         link!
    139 +
    140 +### Example post for LinkedIn:
    141 +
    142 +```
    143 +🚀 Week 2 of AI Dev Tools Zoomcamp by @DataTalksClub complete!
    144 +
    145 +Just built a real-time collaborative coding interview platform usin
         g AI assistants!
    146 +
    147 +Today I learned how to:
    148 +
    149 +- ✅ Build full-stack applications with AI (frontend + backend)
    150 +- ✅ Implement real-time collaboration with WebSockets
    151 +- ✅ Add syntax highlighting for multiple languages
    152 +- ✅ Execute code safely in the browser with WASM
    153 +- ✅ Containerize and deploy the application
    154 +
    155 +Here's my repo: <LINK>
    156 +Demo video: <VIDEO_LINK>
    157 +
    158 +Following along with this amazing course - who else is building wit
         h AI?
    159 +
    160 +You can sign up here: https://github.com/DataTalksClub/ai-dev-tools
         -zoomcamp/
    161 +```
    162 +
    163 +### Example post for Twitter/X:
    164 +
    165 +```
    166 +🤖 Built a collaborative coding platform with AI!
    167 +
    168 +🔗 Shareable links
    169 +⚡ Real-time collaboration
    170 +🎨 Syntax highlighting
    171 +🚀 Browser code execution
    172 +
    173 +My repo: <LINK>
    174 +Demo: <VIDEO_LINK>
    175 +
    176 +Join me: https://github.com/DataTalksClub/ai-dev-tools-zoomcamp/
    177 +```

• Added docs/TODO.md (+12 -0)
     1 +# TODO (docs)
     2 +
     3 +Синхронизировано с корневым `TODO.md`. Кратко:
     4 +
     5 +1. Scaffold client (Vite+React) и server (Express+Socket.IO)
     6 +2. Реалтайм события: join, code:update (комнаты по sessionId)
     7 +3. Редактор CodeMirror (JS/Python), переключатель языка
     8 +4. Выполнение кода: JS (worker), Python (Pyodide worker)
     9 +5. Интеграционные тесты (HTTP+WS)
    10 +6. Единый dev (`npm run dev`), билд и раздача статики
    11 +7. Dockerfile, деплой
    12 +8. Документация + ответы на Q1–Q7

• Edited README.md (+12 -5)
     7
     8 -## Быстрый старт (план)
     9 -- Dev: `npm run dev` (root) — запускает client и server (через `conc
        urrently`).
    10 -- Тесты сервера: `npm --prefix server test`.
    11 -- Сборка клиента: `npm --prefix client run build` (статические файлы
         раздаются Express).
     8 +## Быстрый старт
     9 +- Dev (оба): `npm run dev` — запускает client и server через `concur
        rently`.
    10 +- Тесты сервера: `npm --prefix server test` (Vitest, HTTP+WS integra
        tion).
    11 +- Сборка клиента: `npm --prefix client run build` (Express раздаёт `
        server/public`).
    12 +- Docker (локально): `docker build -t hw2 . && docker run -p 8080:80
        80 hw2`.
    13
       ⋮
    26  ## Roadmap (высокоуровневый)
    26 -- Реалтайм Socket.IO → Редактор CodeMirror → Выполнение JS/Pyodide →
         Тесты → Единый dev → Билд/статик → Docker → Деплой.
    27 + - Реалтайм Socket.IO → Редактор CodeMirror → Выполнение JS/Pyodide
        → Тесты → Единый dev → Билд/статик → Docker → Деплой.
    28 +
    29 +## Ответы ДЗ (ориентиры)
    30 +- Q2 (тесты): `npm --prefix server test`
    31 +- Q3 (оба): `npm run dev` (`concurrently -k -n client,server "npm:de
        v:client" "npm:dev:server"`)
    32 +- Q4 (подсветка): CodeMirror 6 (`@uiw/react-codemirror`, `@codemirro
        r/lang-*`)
    33 +- Q5 (Python→WASM): Pyodide (через web worker)

• Ran ls -la && echo '
  │ server:' && ls -la server && echo '
  │ client:' && ls -la client && echo '
  │ … +3 lines
  └ total 32
    drwxr-xr-x  13 nk.myg  staff   416 Dec 10 01:57 .
    … +56 lines
    -rw-r--r--   1 nk.myg  staff    611 Dec 10 01:57 TODO.md
    drwxr-xr-x   4 nk.myg  staff    128 Dec 10 01:50 source_materials

• Added AGENTS.md (+34 -0)
     1 +# Repository Guidelines
     2 +
     3 +## Project Structure & Module Organization
     4 +- `client/` — React + Vite frontend with CodeMirror editor and Pyodi
        de worker.
     5 +- `server/` — Node.js + Express + Socket.IO backend (rooms by `sessi
        onId`).
     6 +- `tests/` — integration tests (HTTP + WebSocket). Script tests in `
        scripts/tests/`.
     7 +- `scripts/` — helper scripts; `scripts/utils/` for shared bash help
        ers.
     8 +- `conf/` — environment and YAML config examples.
     9 +- `docs/` — documentation, plan, status, prompts log, and homework s
        ource.
    10 +
    11 +## Build, Test, and Development Commands
    12 +- Dev (both): `npm run dev` — runs client and server concurrently.
    13 +- Tests (server): `npm --prefix server test` — Vitest integration te
        sts.
    14 +- Build client: `npm --prefix client run build` — static assets unde
        r `server/public/`.
    15 +- Bootstrap: `bash scripts/bootstrap.sh` — install sub-project deps.
    16 +
    17 +## Coding Style & Naming Conventions
    18 +- JS/TS: 2-space indent, Prettier-like formatting; React components
        `PascalCase`, variables/functions `camelCase`.
    19 +- Python scripts: 4 spaces; Black (88), Ruff if added; modules `snak
        e_case.py`.
    20 +- Keep functions small; add brief JSDoc/docstrings for non-trivial l
        ogic.
    21 +
    22 +## Testing Guidelines
    23 +- Focus on integration first: join and code propagation over Socket.
        IO.
    24 +- Place server tests in `server/tests/` with `*.test.js` or `*.test.
        ts`.
    25 +- Aim for ≥80% coverage on changed code; avoid flakiness (timeouts,
        retries).
    26 +
    27 +## Commit & Pull Request Guidelines
    28 +- Conventional Commits: `feat:`, `fix:`, `chore:`, `docs:`, `test:`,
         `refactor:`.
    29 +- PRs: clear what/why, linked issues (`Closes #id`), screenshots if
        UI, tests/docs updated.
    30 +
    31 +## Agent-Specific Tips
    32 +- Make surgical changes; avoid renames/moves unless necessary.
    33 +- Document new commands in `README.md` and update `PROMPTS-LOG.md` a
        fter major steps.
    34 +- Don’t add dependencies without rationale; keep the solution simple
         and robust.

• Added PLAN.md (+11 -0)
     1 +# План реализации — см. также docs/TODO.md
     2 +
     3 +1) Client/Server scaffold
     4 +2) Socket.IO realtime (join/code:update)
     5 +3) CodeMirror editor (JS/Python)
     6 +4) JS worker + Pyodide worker
     7 +5) Integration tests (HTTP+WS)
     8 +6) Unified dev via concurrently
     9 +7) Build + static serve
    10 +8) Dockerfile (node:20-alpine)
    11 +9) Deploy target (Render/Railway/Fly.io)

• Added STATUS.md (+8 -0)
    1 +# Статус проекта (см. также docs/README.md)
    2 +
    3 +Updated: 2025-12-09
    4 +
    5 +- Скелет client/server, тесты, Dockerfile добавлены.
    6 +- Следующий шаг: запуск локально, уточнение CI и деплоя.
    7 +
    8 +Риски: стабильность Pyodide в воркере, флейки в WebSocket-тестах.

• Added PROMPTS-LOG.md (+3 -0)
    1 +# PROMPTS LOG (root)
    2 +
    3 +Смотрите также `docs/PROMPTS-LOG.md`. Фиксируйте: timestamp, промпт,
       шаг/план, метод, результат, проблемы, next, commit.

• Added client/tsconfig.json (+16 -0)
     1 +{
     2 +  "compilerOptions": {
     3 +    "target": "ES2020",
     4 +    "useDefineForClassFields": true,
     5 +    "lib": ["ES2020", "DOM", "DOM.Iterable"],
     6 +    "module": "ESNext",
     7 +    "skipLibCheck": true,
     8 +    "jsx": "react-jsx",
     9 +    "moduleResolution": "Bundler",
    10 +    "resolveJsonModule": true,
    11 +    "isolatedModules": true,
    12 +    "noEmit": true,
    13 +    "strict": true
    14 +  },
    15 +  "include": ["src"]
    16 +}

─ Worked for 2m 51s ────────────────────────────────────────────────────────

• Implemented the project scaffold end-to-end using the homework spec and
  docs/TODO.

  What I Built

  - Full stack structure
      - client/ (Vite + React + CodeMirror + Socket.IO client + JS/Pyodide
        workers)
      - server/ (Express + Socket.IO, rooms by sessionId, static serve for
        built client)
      - Integration tests (Vitest, HTTP+WebSocket): server/tests/
        socket.test.js
      - Root scripts: npm run dev runs both via concurrently
      - Dockerfile: single image, base node:20-alpine
  - Docs and config
      - docs/source_materials/homework.md (homework text)
      - docs/TODO.md (sync with plan)
      - Root: AGENTS.md, README.md, PLAN.md, STATUS.md, PROMPTS-LOG.md
      - conf/.env.example, conf/config.example.yaml
      - Scripts: scripts/bootstrap.sh, scripts/tests/smoke.sh, scripts/
        utils/print_header.sh
      - .gitignore

  Key Commands

  - Dev (both): npm run dev
  - Tests: npm --prefix server test
  - Build client: npm --prefix client run build
  - Docker: docker build -t hw2 . && docker run -p 8080:8080 hw2

  Tech Choices (answers-ready)

  - Realtime: Socket.IO (join, code:update)
  - Syntax highlighting: CodeMirror 6 (@uiw/react-codemirror, @codemirror/
    lang-*)
  - Python in browser: Pyodide (web worker)
  - Base image: node:20-alpine

  If you want, I can:

  - Add CI (GitHub Actions) for test/build
  - Wire environment (VITE_API_BASE) through .env files
  - Scaffold deployment for Render/Railway/Fly.io

 
› Write tests for @filename
 
  90% context left · ? for shortcuts


